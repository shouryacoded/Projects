#include<bits/stdc++.h>
using namespace std;
int main()
{
    system("cls");
    string name;
    cout << "Enter a string" <<endl;
    getline(cin, name);
    cout << "The length of the string entered is : " << (char) name.length() << endl;
    // char typecasting is shown in the above line
    return 0;
}